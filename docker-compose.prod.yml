services:
  api:
    image: quietjbs/sha256:buscatcher-latest
    container_name: buscatcher-api
    restart: unless-stopped
    environment:
      - PORT=3001
      - DATA_DIR=/app/data
      - ADMIN_TOKEN=${ADMIN_TOKEN:-buscatcher-admin}
    volumes:
      - gtfs-data:/app/data
    networks:
      - buscatcher-network

  nginx:
    image: quietjbs/sha256:buscatcher-latest-nginx
    container_name: buscatcher-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - api
    networks:
      - buscatcher-network

networks:
  buscatcher-network:
    name: buscatcher-network

volumes:
  gtfs-data:
